# 🌊 Океан 2048 — единый план разработки «от и до»

> Единый проработанный план: что нравится людям → полная сверка с документацией
> VK Games → социальные механики → пошаговая реализация «одной нитью мысли».
> Дополняет `ROADMAP.md` (дорожная карта фич) и `store/VK_GAMES.md` (гайд публикации).
> Обновлён: 2026-08-21. Основан на живой документации dev.vk.com (собрана
> playwright-скрейпером в `store/_vk-docs/`, 29 страниц) и актуальных типах
> `@vkontakte/vk-bridge@3.0.2`.

---

## 0. Резюме «одной нитью мысли»

Главная цель игры — **retention**: время в сессии и возврат на следующий день.
Логическая цепочка, по которой строится вся разработка:

```
Валидная интеграция VK (ничего не сломано, без удалённых API)
        ↓
Визуальная сочность (игра «дорогая» на вид, каждое действие — отклик)
        ↓
Социальное соревнование (VK = соцсеть: друзья, приглашения, таблица, истории)
        ↓
Каталог VK (модерация) → открываются серверные механики (лента, миссии, уведомления)
        ↓
Прогрессия и монетизация (прокачка, режимы, игровые покупки)
        ↓
Своя 24/7-экосистема (перенос хостинга, бэкенд, аналитика)
```

Каждый шаг — законченный кусок с тестами, без поломки платформ
(VK Mini Apps / Яндекс Игры / Web / нативные сборки).

---

## 1. Популярные механики: что нравится людям и применимо к нашей игре

Объединяет (а) отраслевой опыт казуалок/2048 и (б) **официальную статистику VK**
из документации игровой платформы (`_vk-docs/`). Применимость подтверждена
цифрами платформы — это то, что реально возвращает и приводит игроков.

| # | Механика | Почему нравится | Статистика VK | Статус в игре |
|---|----------|-----------------|---------------|---------------|
| 1 | **Лёгкий старт** — 0 действий до игры | «Прямо с иконки», без прокруток | иконка на главном экране / левое меню — самые частые места запуска | ⚠️ иконки/меню не предлагаем |
| 2 | **Приглашения друзей** | играть/соревноваться вместе | **до 15% всех запусков, до 25% мобильного трафика** | ❌ нет |
| 3 | **Запросы (вызов друга/помощь)** | прямая коммуникация, «догони меня» | **до 20% запусков** | ❌ нет |
| 4 | **Лента активности друзей** | напоминает об игре друзьям | **до 20% установок, до 15% запусков** | ❌ нет (серверное, после каталога) |
| 5 | **Таблица результатов** | дух соревнования «кто лучше» | платформенная таблица результатов | ❌ критичный гэп (см. §2) |
| 6 | **Истории/шеринг достижений** | показать прогресс друзьям | истории видят друзья, переход по ссылке | ⚠️ только VKWebAppShare |
| 7 | **Ежедневный возврат** (бонус/задания/серия) | «зайди завтра, не потеряй серию» | базовая практика удержания | ✅ ежедневный бонус/задания есть |
| 8 | **Короткие сессии + «ещё один ход»** | партии 2048 короткие, зацепка после каждой | основа жанра | ✅ |
| 9 | **Прогрессия и награды** | всегда в паре шагов от награды | 7 глубин, достижения | ✅ |
| 10 | **Визуальная сочность** (анимации, частицы) | «дорогая» казуалка, эмоция от каждого хода | качественная графика — требование модерации | ⚠️ Фаза 1 ROADMAP |
| 11 | **Уведомления/возврат неактивных** | вернуть тех, кто ушёл | разовые / неактивным / всем | ❌ нет (серверное) |
| 12 | **Миссии** (соц. механика) | цель ≠ «набери N», история | сообщения о миссиях в ленте друзей | ❌ нет (серверное) |

**Вывод:** топ-механики VK — это **социальные** (приглашения 15–25%, запросы 20%,
лента 15–20%). Они напрямую бьют в retention и недоступны в нашей игре.
Их внедрение — главный приоритет после закрытия критичного гэпа лидерборда.

---

## 2. Полная сверка документации VK Games с игрой (gap-анализ)

Источник: живые страницы dev.vk.com (разделы «VK Games» → «Игровые и социальные
механики», «Монетизация», «Реклама в играх», «Первые шаги», «Запуск игры»)
и актуальные типы `@vkontakte/vk-bridge@3.0.2` (`data.d.ts`).

> ⚠️ **КРИТИЧНО:** методы `VKWebAppSaveToLeaderBoard` и `VKWebAppGetLeaderBoard`,
> которые использует наш [`js/platform-sdk.js`](js/platform-sdk.js:279),
> **УДАЛЕНЫ из vk-bridge** (проверено в 3.0.2 и 2.2.2 — их нет в `RequestPropsMap`).
> Актуальный механизм VK — серверный `secure.addAppEvent` (запись) +
> `VKWebAppShowLeaderBoardBox` (показ системной таблицы). См. §2.1.

### 2.1. Лидерборды VK — как это работает сейчас (из документации)

Страница `dev.vk.com/ru/games/promotion/game-mechanics/leaderboards`:

1. **Тип таблицы** выбирается в настройках игры (Настройки → Дополнительные →
   Таблица результатов): по уровням / очкам / баллам миссий.
   Совет VK: «Уровни — для три-в-ряд и сюжетных; Очки — для аркад и рекордов».
   → для нас: **Очки**.
2. **Запись достижений** — серверный метод **`secure.addAppEvent`**:
   - очки: `activity_id = 2`, `value` от 1 до 10 000 000, **каждое значение строго
     больше предыдущего** (иначе ошибка);
   - уровни: `activity_id = 1`, от 1 до 15 000;
   - работает **только после публикации в каталоге**;
   - требует **сервисный ключ доступа** (сервер, НЕ клиент).
3. **Показ таблицы** — клиентское событие **`VKWebAppShowLeaderBoardBox`** с
   параметром `user_result` (уровень/очки/баллы миссий). Открывает **системную
   таблицу VK** с друзьями. Рекомендуется показывать после окончания сессии.
4. **Чтение таблицы в свою UI-таблицу** — клиентского API НЕТ. VK больше не
   отдаёт список записей (в отличие от старого `VKWebAppGetLeaderBoard`).

**Что это значит для нашей игры:**

| Было (не работает) | Стало (по документации VK) |
|--------------------|------------------------------|
| `VKWebAppSaveToLeaderBoard` (запись очков) | `secure.addAppEvent` (сервер, `activity_id=2`) — после каталога |
| `VKWebAppGetLeaderBoard` (чтение в свою таблицу) | нет клиентского чтения → показываем **системную таблицу** через `VKWebAppShowLeaderBoardBox` |
| своя кастомная таблица с именами друзей | системная таблица VK (друзья/все) |

**План замены (`js/platform-sdk.js`):**
- `submitScore(score, level)` на VK: (а) сейчас — мягко «заглушить»/отказаться от
  удалённого вызова, чтобы не было ошибок в консоли; (б) реализовать
  `VKWebAppCallAPIMethod` с `secure.addAppEvent` НЕЛЬЗЯ (нужен сервисный ключ) →
  после переноса на свою экосистему добавить серверную запись; (в) сразу добавить
  новый метод `showLeaderboard(score)` → `VKWebAppShowLeaderBoardBox {user_result}`.
- `getLeaderboard()` на VK: удалить вызов удалённого метода; вместо своей таблицы
  на VK показывать системную `VKWebAppShowLeaderBoardBox` (кнопка «Таблица»).
- На Яндексе и вебе кастомная таблица остаётся (там API лидербордов живой).

### 2.2. Таблица сверки всех возможностей VK

| # | Возможность | Официальный API | Статус в игре | Действие |
|---|-------------|-----------------|---------------|----------|
| 1 | Инициализация | `VKWebAppInit` (обязателен, до 30 с) | ✅ `sdk.init()` | — |
| 2 | Автоопределение хоста | параметры запуска (`vk_user_id`, `vk_app_id`, …) | ✅ `detectHost()` | — |
| 3 | Облачные сохранения | `VKWebAppStorageSet/Get/GetKeys` (ключ `ocean2048_save`) | ✅ `saveCloud/loadCloud` | — |
| 4 | Запись в таблицу | `secure.addAppEvent` (`activity_id=2`, сервер, после каталога) | ❌ удалённый `VKWebAppSaveToLeaderBoard` | **Заменить** (§2.1) |
| 5 | Показ таблицы | `VKWebAppShowLeaderBoardBox` (`user_result`) | ❌ нет | **Добавить** |
| 6 | Чтение таблицы в свою UI | — (нет клиентского API) | ❌ удалённый `VKWebAppGetLeaderBoard` | Убрать → системная таблица |
| 7 | Реклама interstitial | `VKWebAppShowNativeAds` (`interstitial`) | ✅ | — |
| 8 | Реклама rewarded | `VKWebAppShowNativeAds` (`reward`) | ✅ | — |
| 9 | Баннерная реклама | `VKWebAppShowBannerAd` | ❌ нет | Опция (после каталога) |
| 10 | Поделиться | `VKWebAppShare` | ✅ | — |
| 11 | Приглашения | `VKWebAppShowInviteBox` (+ `requestKey`) | ❌ нет | **Фаза 2** (соц) |
| 12 | Запросы | `VKWebAppShowRequestBox` + `friends.getAppUsers` | ❌ нет | **Фаза 2** (соц) |
| 13 | Истории | `VKWebAppShowStoryBox` (шаблон достижения) | ❌ нет | **Фаза 2** (соц) |
| 14 | Добавить в избранное/левое меню | `VKWebAppAddToFavorites` | ❌ нет | **Фаза 2** (соц, +бонус) |
| 15 | На главный экран (Android) | `VKWebAppAddToHomeScreen(+Info)` | ❌ нет | **Фаза 2** (соц, +бонус) |
| 16 | Профиль пользователя | `VKWebAppGetUserInfo` | ❌ нет | Фаза 2 (для соц) |
| 17 | Список друзей | `VKWebAppGetFriends` / `friends.getAppUsers` | ❌ нет | Фаза 2 (для соц) |
| 18 | Контексты запуска | `vk_request_key`, параметры запуска (`#hash`) | ❌ нет | Фаза 2 (диплинки) |
| 19 | Личные сообщения от игры | `VKWebAppAllowMessagesFromGroup` + сервер `messages.send` | ❌ нет | Фаза 4 (сервер, после каталога) |
| 20 | Уведомления | `secure.sendNotification` + `VKWebAppAllowNotifications` | ❌ нет | Фаза 4 (сервер) |
| 21 | Лента активности друзей | `secure.addAppEvent` (`activity_id=1/2`) | ❌ нет | Фаза 4 (сервер, после каталога) |
| 22 | Миссии | `secure.addAppEvent` (id миссии) + панель «Соц. механики» | ❌ нет | Фаза 4 (панель+сервер) |
| 23 | Игровые покупки (голоса) | `VKWebAppShowOrderBox` + сервер уведомлений | ❌ нет | Фаза 6 (после каталога) |
| 24 | Сниппеты | `apps.addSnippet` / настройки «Оформление» | ❌ нет | В кабинете при публикации |
| 25 | Вибрация | `VKWebAppTaptic*` | ❌ нет (есть веб-haptic) | Опция |
| 26 | Тёмная тема/статус-бар | `VKWebAppSetViewSettings` | ❌ нет | Опция (UI) |

**Итого:** базовое (1–3, 7–8, 10) реализовано. **Критичный гэп — лидерборд (4–6).**
**Ценные соц-фичи (11–17) отсутствуют.** Серверные (19–23) — после каталога.

---

## 3. Социальные механики между игроками (проект)

VK — соцсеть, поэтому соревнование и коммуникация между игроками — ключ к
удержанию. Ниже — спроектированные сценарии (с учётом ограничений VK).

> ⚠️ Ограничения VK: приглашения/запросы/лента/миссии/уведомления доступны
> **только после публикации в каталоге**. До каталога можно: добавлять в
> избранное, на главный экран, истории, шаринг, и (между администраторами)
> приглашения/запросы. Код готовим заранее, активируем после публикации.

### 3.1. Соревнование (приоритет №1)
- **Таблица результатов** — системная `VKWebAppShowLeaderBoardBox` после каждой
  партии + кнопка в меню (замена удалённого лидерборда, §2.1).
- **«Обыграй друга»**: после партии показываем системную таблицу — игрок видит,
  что друг его обошёл (VK показывает друзей).
- **Бонус за добавление в избранное / на главный экран** (`VKWebAppAddToFavorites`,
  `VKWebAppAddToHomeScreen`) — «+50 жемчужин», быстрый запуск → чаще возвраты.

### 3.2. Привлечение друзей (приоритет №2)
- **«Пригласить друзей»** в меню и после партии: `VKWebAppShowInviteBox`
  (`requestKey` уникальный на кампанию). VK сам выбирает друзей, которые ещё не
  играли (это повышает конверсию).
- **Вызов друга** `VKWebAppShowRequestBox` с текстом-вызовом («Сможешь обыграть
  мой рекорд 2048?») — кнопка рядом с рекордом.
- **История достижения** `VKWebAppShowStoryBox` — шаблон «Я дошёл до глубины X!
  Рекорд N» с кнопкой «Играть» (attachment). Награда за публикацию.
- **Диплинки/контекст**: при входе по `vk_request_key` — показать соответствующий
  экран (например, «Тебя вызвали на дуэль»).

### 3.3. Возврат и напоминания (приоритет №3, после каталога)
- **Лента активности друзей**: `secure.addAppEvent` при новом рекорде (очки,
  `activity_id=2`) и прохождении глубины (уровни, `activity_id=1`) — друзья
  видят «Друг прошёл уровень „Акула“».
- **Личные сообщения от игры**: `VKWebAppAllowMessagesFromGroup` + сервер
  `messages.send` («Твой друг обошёл тебя в таблице!», «Новая миссия открыта»)
  с переходом по ссылке `[https://vk.com/appID#…|Запустить игру]`.
- **Уведомления**: `secure.sendNotification` неактивным игрокам (3+ дня), счётчик
  `secure.setCounter` на значке игры.
- **Миссии**: создать в панели «Соц. механики → Миссии» (название с глаголом,
  баллы 1–100, лимит 200 миссий / 2000 баллов), отправлять `secure.addAppEvent`
  с `activity_id` миссии — сообщение уходит в ленту друзей.

### 3.4. Порядок внедрения
1. Сначала клиентские (работают до каталога): таблица, избранное, главный экран,
   истории, приглашения/запросы (между админами для теста).
2. Потом серверные (после каталога + перенос на свою экосистему): лента, миссии,
   уведомления, личные сообщения.

---

## 4. Единый пошаговый план разработки «от и до»

Каждая фаза: законченный кусок → тесты → сборка → коммит. Приоритет сверху вниз.
Условные обозначения: ✅ готово / ⏳ в работе / ⬜ план.

### Фаза 0 — Фундамент ✅ (готово)
- База 2048 (свайпы/стрелки/WASD/D-pad), 7 глубин (4×4→6×6).
- Магазин (shuffle/bomb/x2), скины, темы, ежедневный бонус/задания, достижения,
  комбо/серии, облако VK/Яндекс, реклама, звук, анимации-база, reduce-motion.
- VK: init, облако, реклама, шаринг (см. §2.2). Сборки: `build:www/vk/yandex`.

### Фаза 0.5 — Критичные гэпы VK (стабильность) ✅ (готово)
> Закрывает удалённые API — делает VK-интеграцию валидной и без консольных ошибок.
- [x] [`js/platform-sdk.js`](js/platform-sdk.js): убрать `VKWebAppSaveToLeaderBoard`
      и `VKWebAppGetLeaderBoard` (удалены из vk-bridge).
- [x] Добавить `sdk.showLeaderboard(score)` → `VKWebAppShowLeaderBoardBox
      {user_result}`; кнопка «Таблица» в меню + показ после партии.
- [x] `getLeaderboard()` на VK — вернуть `[]`, свою таблицу оставить только на
      Яндексе/вебе (там API жив).
- [x] Юнит-тесты `js/platform-sdk.test.js` под новый API; проверить консоль без 404/ошибок.
- [x] Обновить `store/VK_GAMES.md` (таблица реализованного SDK, чек-лист).
- [x] Коммит «fix(vk): замена удалённых методов лидерборда на VKWebAppShowLeaderBoardBox».

### Фаза 1 — «Визуальное сияние» (ROADMAP Фаза 1) ✅ (готово)
> Самый заметный шаг для эмоции и «дорогого» вида. Не зависит от каталога.
- [x] Плитки-обитатели (2=🐟 … 2048=🐉), цвет/глиф растут с числом.
- [x] Частицы при слиянии (всплеск/брызги, GPU, ≤40 элементов, reduce-motion-фолбэк).
- [x] Пузырьки-фон за доской (CSS, выкл. в reduce-motion).
- [x] Пульс рекорда + счёт-вверх (count-up).
- [x] Тень/глубина: тени плиток, градиент фона.
- [x] Микровибрация/визуальный отклик тача.

### Фаза 2 — Социальные механики VK (клиентские, до каталога) ✅ (готово)
> Прямо бьёт в retention: приглашения до 25% трафика, запросы до 20%.
- [x] `sdk.showInvite(requestKey)` → `VKWebAppShowInviteBox`; кнопка «Пригласить друзей».
- [x] `sdk.showRequest(friendId, text)` → `VKWebAppShowRequestBox`; кнопка «Вызвать друга».
- [x] `sdk.showStory({title, link, image})` → `VKWebAppShowStoryBox`; кнопка «История».
- [x] `sdk.addToFavorites()` → `VKWebAppAddToFavorites` + бонус жемчужинами.
- [x] `sdk.addToHomeScreen()` → `VKWebAppAddToHomeScreen(+Info)` + бонус (Android).
- [x] `sdk.getUserInfo()`, `sdk.getFriends()` → для персонализации.
- [x] Обработка контекстов запуска: `vk_request_key` → нужный экран/тост.
- [x] UI: экран «Друзья»/соц-кнопки в меню и после партии (не чаще 1 диалога между раундами — требование VK).

### Фаза 3 — Публикация в каталог VK ⬜
- [ ] Создать VK Mini App (тип «Игра»), получить App ID.
- [ ] URL в «Размещении» (текущий: 5mb2.ru; после своей экосистемы — свой).
- [ ] Заполнить карточку («Шпаргалка» в `store/VK_GAMES.md`), «Описание основных
      сценариев», иконки/скриншоты, статус «Включено».
- [ ] Подать на модерацию (мин. 3 дня). Каталог открывает серверные механики.

### Фаза 4 — Серверные механики VK (после каталога + своя экосистема) ⬜
- [ ] Бэкенд: эндпоинт записи в таблицу → `secure.addAppEvent` (очки `activity_id=2`,
      уровень `activity_id=1`, только растущие значения).
- [ ] Лента активности: отправка при рекорде/глубине.
- [ ] Миссии: создать в панели VK + серверная отправка по выполнению.
- [ ] Уведомления неактивным: `secure.sendNotification`, `secure.setCounter`.
- [ ] Личные сообщения: `VKWebAppAllowMessagesFromGroup` + `messages.send` (правила
      «3 дня / 5 дней / скидка», не заваливать).

### Фаза 5 — Рост retention (ROADMAP Фазы 2–3) ⬜
- [ ] Сюжетные миссии на глубинах, смягчение кривой сложности (под-цели).
- [ ] Модификаторы («Коралловая волна», «Планктон», «Тёплое течение»).
- [ ] Препятствия и случайные события.
- [ ] Режимы: Бесконечный (отдельная кнопка), Челлендж (N ходов), еженедельные челленджи.

### Фаза 6 — Прогрессия и монетизация ⬜
- [ ] Прокачка ныряльщика (шанс 4, доп. ход, неуязвимость) за жемчужины.
- [ ] Профили (несколько ныряльщиков).
- [ ] Игровые покупки: `VKWebAppShowOrderBox` + сервер уведомлений (голоса).
      Рекомендация VK: разовые списания для валюты, подписки — для VIP-бонусов.
- [ ] Баннерная реклама `VKWebAppShowBannerAd` (опционально).

### Фаза 7 — «Живой океан»: масштаб игры-сервиса (ROADMAP Фаза 7) ⬜
> То, что превращает «2048 с уровнями» в долгоживущую игру-сервис. Крупные идеи
> — после базовых фаз, каждая — отдельный дизайн-док (ROADMAP §5.3).

- [ ] **🧩 Ежедневная головоломка (общий сид)** — всем игрокам одна и та же доска
      в день (фиксированный seed), соревнование за лучший результат. Wordle-механика:
      вирусный рост + ежедневный возврат. **Первая из идей масштаба** — малый объём.
- [ ] **📅 Сезоны + «Журнал глубины» (Battle Pass)** — сезонные события по календарю,
      30–50 ступеней наград, две дорожки (бесплатная / за жемчужины). Возврат каждый
      день сезона.
- [ ] **🌐 Живой Океан (мета-игра)** — карта мирового океана: экспедиции в регионы,
      открытие зон, обитатели с историями. Прогрессия на месяцы, не кончается на 7 глубин.
- [ ] **🏛️ Океанариум (коллекция + дом)** — аквариум с собранными обитателями,
      декорации, витрина профиля. Completionist-драйв.
- [ ] **🗺️ Экспедиции (roguelike)** — процедурные погружения: выбор пути, модификаторы,
      артефакты. Бесконечная реиграбельность.
- [ ] **⚔️ Боссы глубин** — сюжетные боссы с новыми механиками, награда — уникальные
      обитатели в коллекцию.
- [ ] **🌊 Глобальные события сообщества** — общая цель всех игроков за неделю → общая
      награда. Вовлекает комьюнити, стимул приглашать друзей.
- [ ] **🚢 Корабли-гильдии** — соц-объединения игроков, командные челленджи (развитие
      соцмеханик Фазы 2).

### Фаза 8 — Своя 24/7-экосистема ⬜
- [ ] Перенос хостинга `build/vk/` на свой сервер, смена URL в кабинете VK
      (облако `ocean2048_save` и таблица не зависят от URL).
- [ ] Серверный бэкенд: запись в таблицу (`secure.addAppEvent`), платежи, миссии,
      уведомления, личные сообщения.
- [ ] Аналитика: сессии/время/возвраты (свои события + платформенная).

---

## 5. Порядок работ «здесь и сейчас» (ближайшие шаги)

✅ Фазы 0.5, 1 и 2 завершены (стабильность VK, визуал, соцмеханики). Дальше по
приоритету retention:

1. **Фаза 5 (ROADMAP 2)** — рост retention: карта глубин, сюжетные миссии,
   случайные события (см. приоритеты идей в `ROADMAP.md` §3.5).
2. **Фаза 7 (ROADMAP 7)** — быстрые идеи масштаба: **ежедневная головоломка (общий
   сид)** — малый объём, сильный вирусный эффект, ложится на `js/daily.js`.
3. **Фаза 3** — публикация в каталог VK (модерация, открывает серверные механики).
4. **Фаза 4** — серверные механики VK (лента, миссии, уведомления — после каталога).

Правила реализации — из `ROADMAP.md` §5 (ключи БД, океанская тема, retention-тест
каждой фичи, платформенная совместимость через `js/platform-sdk.js`, хостинг VK
временный).
