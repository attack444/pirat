# Яндекс Игры — публикация «Пират 2048»

Гайд по публикации браузерной версии игры на платформе **Яндекс Игры**
(games.yandex.ru). Игра запускается в iframe на Яндексе. SDK v2 подключается по
официальной схеме (https://yandex.ru/dev/games/doc/dg/sdk/sdk-about.html,
п. 1.1 требований платформы): относительный путь `/sdk.js` — проксируется
платформой для игр, загруженных архивом на сервер Яндекса. Путь
`https://yandex.ru/games/sdk/v2` больше не используется.

Обновлён: 2026-08-19.

## Что уже реализовано (SDK-адаптер `js/platform-sdk.js`)

| Возможность | API Яндекса | Статус |
|-------------|-------------|--------|
| Инициализация | `YaGames.init()` + `getPlayer({ scopes: false })` | ✅ |
| Загрузочный экран | `ysdk.features.LoadingAPI.setLoadingProgress/ready` | ✅ |
| Облачные сохранения | `player.setData/getData` (ключ `pirat2048`) | ✅ |
| Лидерборд | `getLeaderboards().setLeaderboardScore('pirat2048_top', ...)` | ✅ |
| Реклама interstitial | `ysdk.adv.showFullscreenAdv` | ✅ |
| Реклама rewarded | `ysdk.adv.showRewardedVideo` | ✅ |
| Поделиться | `navigator.share` / clipboard | ✅ |
| Service Worker на площадке | — | ✅ не регистрируется (только standalone-веб) |

Все методы безопасны: без SDK / без авторизации / без модерации рекламы они
возвращают `false`/пустые данные, игра продолжает работать (web-режим).

## Подключение SDK (официальная схема, п. 1.1 требований платформы)

Источник: https://yandex.ru/dev/games/doc/dg/sdk/sdk-about.html — раздел «Подключение».

1. В `index.html` в `<head>` добавлен статический тег — модератор видит, что SDK
   «встроен» в игру:
   ```html
   <script async src="/sdk.js" onload="window.__yaSdkScriptLoaded = true"></script>
   ```
2. В `js/platform-sdk.js` (`ensureYaGames()`) подключается только относительный
   `/sdk.js` — проксируется платформой для архивных игр (рекомендуемый путь,
   проверяется модерацией: индикатор загрузчика `IT`);
   - S3-фолбэк `https://sdk.games.s3.yandex.net/sdk.js` **удалён** — абсолютные
     URL на серверы S3 Яндекса запрещены в коде (п. 1.7 требований платформы);
   - `YaGames.init()` вызывается только после загрузки скрипта и обёрнут в
     `withTimeout(5 с)` — игра не зависнет на лоадере, если init не вернётся.
3. ❌ `https://yandex.ru/games/sdk/v2` (старый путь) **удалён** — именно он был
   причиной отклонения «Не встроено или некорректно встроено SDK» (п. 1.1).
4. Индикатор загрузчика (Loader Indicator): `IT` (новый загрузчик, корректный),
   `IF` — старый.

## Проверка по требованиям платформы (аудит 19.08.2026)

Пройдены все требования официальных доков (`concepts/requirements.md`, `ru/requirements/*`),
кроме п. 1.1 (SDK — исправлен ранее). Итог:

| Пункт | Требование | Как выполнено |
|-------|-----------|---------------|
| 1.7 | В коде нет абсолютных URL на S3-серверы Яндекса | ✅ S3-фолбэк удалён из `ensureYaGames()` (код и комментарии), тест проверяет отсутствие `s3`/`yandex.net` |
| 2.14 | Автоопределение языка при запуске (`ysdk.environment.i18n.lang`), а не в игре | ✅ `sdk.getLang()` (заполняется в `init()`), в `main.js` при старте `document.documentElement.lang = lang` |
| 4.7 | Пауза звука и геймплея при показе полноэкранной рекламы | ✅ `runWithAdPause()` вокруг 4 вызовов рекламы: `suspendSound()` + `game.setPaused(true)` |
| 1.3 | Звук ставится на паузу при потере фокуса (свернуть вкладку/окно, меню вкладок) | ✅ `suspendSound()`/`resumeSound()` на `visibilitychange`, `blur`/`focus` |
| 1.6.1.8/1.6.2.7 | Поле не выделяется, не открывается контекстное меню (долгое нажатие/ПКМ) | ✅ `contextmenu` preventDefault на `.board, .dpad` + `-webkit-touch-callout: none` (глобально `user-select: none`) |
| 1.10.2 | Нет браузерной прокрутки/swipe-to-refresh | ✅ `html, body { overflow: hidden; overscroll-behavior: none }` + своя прокрутка `.container` |
| 1.19.2 | `LoadingAPI.ready()` в момент старта игры | ✅ уже было корректно (после старта уровня, до скрытия лоадера) |
| 1.19.3/1.19.4 | GameplayAPI / `ysdk.on`-пауза | ➖ не обязательны — эти API не используются |

Код-точки: `suspendSound`/`resumeSound` — `js/sound.js`; `runWithAdPause` — `js/main.js`;
`lang`/`getLang` — `js/platform-sdk.js`; CSS — `css/styles.css`.

## Сборка (локально, Windows)

```bash
npm run build:yandex
# → build/yandex/  (index.html, manifest.json, sw.js, privacy-policy.html,
#   css/styles.css, icons/, js/ — 14 модулей, без тестов и мусора)

powershell -Command "Compress-Archive -Path 'D:\pirat\build\yandex\*' -DestinationPath 'D:\pirat\build\yandex.zip' -Force"
# → build/yandex.zip (≈92 КБ) — готов для загрузки в кабинет
```

ZIP **уже собран и актуален** (пересобран 19.08.2026 после аудита по требованиям
платформы: S3-фолбэк удалён, добавлены язык/пауза звука/контекстное меню/своя
прокрутка): `build/yandex.zip`, 92414 байт. Внутри:
- `index.html` лежит в корне (требование Яндекса);
- `js/main.js` — актуальный, с условием `platform.isWeb && sdk.host === 'web'`
  (на площадке SW не регистрируется);
- `sw.js` включён (PWA), но на Яндексе не срабатывает;
- `icons/icon-512.png` — иконка 512×512 для кабинета;
- `privacy-policy.html` — политика конфиденциальности (нужна для раздела «Конфиденциальность»).

Повторная сборка/переархивация (если меняли код):
```bash
npm run build:yandex && powershell -Command "Compress-Archive -Path 'D:\pirat\build\yandex\*' -DestinationPath 'D:\pirat\build\yandex.zip' -Force"
```

## Шпаргалка для черновика (копируй-вставляй)

> Ниже — поля формы «Создание игры» в кабинете и готовые значения.
> Значения с пометкой **(проверить)** укажи от себя.

### Тексты

**Название игры**
```
Пират 2048
```

**Короткое описание (одной строкой)**
```
Классическая 2048 в пиратском стиле: 7 уровней, свайпы, без рекламы. Соединяй плитки и стань Пиратским Королём!
```

**Полное описание**
```
🏴☠️ Пират 2048 — классическая головоломка 2048 в пиратском стиле.

• 7 уровней-рангов: от Юнги до Пиратского Короля
• Поля 4×4, 5×5 и 6×6 — сложность растёт
• Управление: свайпы и кнопки-стрелки
• Магазин скинов и бустов, ежедневный бонус
• Облачные сохранения прогресса
• Без покупок, без интернета для игры

Соединяй одинаковые плитки, набирай очки, открывай скины и новые ранги!
```

**SEO-описание (для поиска/метаданных)**
```
Пират 2048 — бесплатная браузерная головоломка: соединяй одинаковые плитки на поле 4×4, 5×5 и 6×6, проходи 7 пиратских рангов, открывай скины и бусты. Играй без рекламы и регистрации на любом устройстве.
```

**Как играть**
```
Соединяй плитки с одинаковыми числами, сдвигая их стрелками или свайпами (влево, вправо, вверх, вниз). Две одинаковые плитки сливаются в одну — число удваивается. Набирай очки, проходи ранги от Юнги до Пиратского Короля и открывай новые поля 4×4, 5×5 и 6×6. В магазине можно купить скины и бусты за дублоны, а ежедневный бонус даёт награды за каждый вход. Игра сохраняет прогресс автоматически — в облако и на устройство.
```

**Теги / ключевые слова**
```
2048, головоломка, пират, числа, пазл, логика, свайпы, пират 2048, игра 2048, головоломки, логические игры, бесплатные игры, браузерные игры
```

### Настройки

| Поле | Значение |
|------|----------|
| Тип игры | Браузерная |
| Категория | Головоломки (Puzzle) |
| Язык | Русский |
| Возрастной рейтинг | 3+ |
| Поддерживаемые платформы | Windows, macOS, Linux, Android, iOS |
| Политика конфиденциальности | `https://5mb2.ru/static/games/pirate-2048/privacy-policy.html` |
| Сайт игры | `https://5mb2.ru/games/game/pirate-2048` |
| E-mail поддержки | **(проверить)** свой e-mail |
| Лидерборд | `pirat2048_top` (создать в настройках) |

### Возрастные ограничения (чекбоксы — всё «нет»)

- [ ] Насилие / кровь
- [ ] Сексуальный контент
- [ ] Ненормативная лексика
- [ ] Азартные игры / ставки на реальные деньги
- [ ] Покупки за реальные деньги внутри игры
- [ ] Общение пользователей / контент из интернета

### Монетизация

| Поле | Значение |
|------|----------|
| Реклама interstitial | вкл. (показ между партиями) |
| Реклама rewarded | вкл. (награда за просмотр — магазин/ежедневный бонус) |
| Платёжный аккаунт | подключить (самозанятость/ИП/ООО) — иначе выплаты не идут |

### Графика (файлы)

| Поле | Файл |
|------|------|
| Иконка | `D:\pirat\build\yandex\icons\icon-512.png` (512×512) |
| Скриншоты (мин. 2, лучше 4, 1280×720) | `D:\pirat\store\shots\yandex-home.png`, `yandex-moves.png`, `yandex-shop.png`, `yandex-shop-skin.png` |
| Обложка (cover) | `D:\pirat\store\shots\yandex-cover.png` (**800×470**, готово) |
| Превью (видео 9:16) | опционально — можно пропустить |

### Файлы игры

| Поле | Файл |
|------|------|
| Архив (ZIP) | `D:\pirat\build\yandex.zip` (92 КБ) |

## Пошаговая инструкция отправки в кабинет (детально)

> Адрес кабинета: **https://games.yandex.ru** (войти по Яндекс ID; для создания игры
> нужно подтвердить аккаунт разработчика — номер телефона).

### Шаг 1. Создать игру
1. На главной кабинета нажать **«Добавить игру»** / **«Создать игру»**.
2. Выбрать тип **«Браузерная игра»** (не «Мобильная»).
3. Указать название: **Пират 2048**.

### Шаг 2. Загрузить игровой файл (ZIP)
1. Открыть вкладку/раздел **«Игровые файлы»** (или «Архив игры»).
2. Загрузить `D:\pirat\build\yandex.zip` (**92 КБ**).
3. Требование Яндекса выполнено: `index.html` в корне архива.
4. Нажать «Сохранить»/«Проверить» — кабинет распакует архив и покажет структуру.

### Шаг 3. Заполнить метаданные (страница «Основное»/«Описание»)
| Поле | Значение |
|------|----------|
| Название | Пират 2048 |
| Короткое описание | Классическая 2048 в пиратском стиле: 7 уровней, без рекламы. |
| Полное описание | то же, что в `GOOGLE_PLAY.md` (полное описание) |
| Категория | Головоломки (Puzzle) |
| Теги | 2048, головоломка, пират, числа, пазл |
| Язык | Русский |
| Возрастной рейтинг | 3+ (без насилия, азарта и покупок) |
| Платформа | Браузер (desktop + mobile) |
| Ссылка на политику конфиденциальности | `https://5mb2.ru/static/games/pirate-2048/privacy-policy.html` (или `privacy-policy.html` из архива) |

### Шаг 4. Загрузить графику
1. **Иконка**: `D:\pirat\build\yandex\icons\icon-512.png` (512×512).
2. **Скриншоты** (мин. 2, лучше 4, размер **1280×720**):
   - `D:\pirat\store\shots\yandex-home.png`
   - `D:\pirat\store\shots\yandex-moves.png`
   - `D:\pirat\store\shots\yandex-shop.png`
   - `D:\pirat\store\shots\yandex-shop-skin.png`
3. Превью (видео/анимация 9:16) — опционально, можно пропустить.

### Шаг 5. Настроить лидерборд и облако
1. В настройках игры найти раздел **«Лидерборды»** (или «Таблица рекордов»).
2. Создать лидерборд с именем **`pirat2048_top`** (тип «Баллы», сортировка по убыванию).
   - Имя должно совпадать с кодом: `sdk.getLeaderboards().setLeaderboardScore('pirat2048_top', score)`.
   - Если лидерборд не создать — код безопасно пропустит запись, игра не сломается.

### Шаг 6. Монетизация (реклама)
1. В разделе **«Монетизация»** / «Реклама» включить:
   - **interstitial** (полноэкранная между партиями),
   - **rewarded** (за просмотр — награда в магазине/за ежедневный бонус).
2. Для выплат Яндекс потребует **подключить платёжный аккаунт** (для РФ — самозанятость
   или ИП/ООО). Без него реклама может не показываться даже после модерации.
3. До модерации рекламы методы `showInterstitial`/`showRewarded` возвращают `false` —
   игра работает в обычном режиме без сбоев.

### Шаг 7. Тест в песочнице (обязательно)
1. В кабинете нажать **«Запустить»** / открыть песочницу (игра откроется в тестовом iframe).
2. Проверить (подробный чек-лист — в разделе «Тест в песочнице» ниже).
3. Если всё работает — **«Отправить на модерацию»**.

### Шаг 8. Модерация
- Обычно 1–3 рабочих дня. При отклонении придёт причина — см. «Типичные причины отклонения».
- После одобрения игра появится в поиске/каталоге и начнёт показывать рекламу.

## Тест в песочнице (перед отправкой)

- [ ] Игра запускается, лоадер доходит до 100% (`LoadingAPI.setLoadingProgress`).
- [ ] Экран «Играть»/меню отображается, шрифты не плывут.
- [ ] Новая партия: свайпы/стрелки работают, плитки двигаются.
- [ ] Кнопка **«Магазин»** открывается: вкладки «Бонусы» и «Скины», покупка/выбор скина.
- [ ] Ежедневный бонус открывается, начисление работает.
- [ ] Реклама (если уже разрешена) появляется по триггерам; rewarded — после просмотра.
- [ ] Очки записываются в лидерборд `pirat2048_top` (видно в тесте, если создан).
- [ ] Облачное сохранение: прогресс переживает перезагрузку в другом окне/браузере.
- [ ] Кнопка «На весь экран» не роняет iframe (код игнорирует отказ).
- [ ] Никаких консольных ошибок (F12 в песочнице).
- [ ] SDK загружен: нет ошибок `Failed to load resource: /sdk.js`, нет запросов к `yandex.ru/games/sdk/v2` и `sdk.games.s3.yandex.net`; `YaGames.init()` отрабатывает (лоадер доходит до 100%).
- [ ] Язык (п. 2.14): в отладочной панели индикатор `文` — зелёный (язык определён при запуске через `ysdk.environment.i18n.lang`); `<html lang>` соответствует языку.
- [ ] Контекстное меню (п. 1.6.1.8/1.6.2.7): ПКМ и долгое нажатие на поле/кнопках-стрелках не открывают меню браузера, поле не выделяется.
- [ ] Пауза звука (п. 1.3/4.7): при сворачивании/переключении вкладки и при показе полноэкранной рекламы звук и геймплей ставятся на паузу; после возврата/закрытия — возобновляются.
- [ ] Скролл (п. 1.10.2): нет браузерной прокрутки страницы и swipe-to-refresh; контент прокручивается только внутри игры.

## Типичные причины отклонения (и как у нас)

| Причина | Статус у нас |
|---------|-------------|
| SDK не встроен / встроен некорректно (п. 1.1) | ✅ исправлено 19.08: статический `<script async src="/sdk.js">` в `<head>` + `ensureYaGames()` (только `/sdk.js`); `yandex.ru/games/sdk/v2` и S3-фолбэк удалены (п. 1.7) |
| Ошибка запуска/не открывается index.html в корне | ✅ index.html в корне архива |
| Сломанная вёрстка/нечитаемые тексты | ✅ проверено на скриншотах 1280×720 |
| Ошибки консоли / краши | ✅ тесты 182/182, песочница |
| Нет политики конфиденциальности | ✅ `privacy-policy.html` в архиве + на 5mb2.ru |
| Реклама без модерации/нарушение | ✅ до модерации реклама не показывается |
| Некорректный возрастной рейтинг | ✅ 3+, без насилия/азарта |

## Чеклист готовности

- [x] SDK-адаптер (init / LoadingAPI / player / лидерборд / реклама / share)
- [x] Загрузочный экран с прогрессом (LoadingAPI)
- [x] Облачные сохранения (setData/getData)
- [x] Регистрация SW отключена на площадке
- [x] SDK встроен по официальной схеме: статический `<script async src="/sdk.js">` в `<head>` + `ensureYaGames()` (только `/sdk.js`), без S3 и `yandex.ru/games/sdk/v2`
- [x] Соответствие требованиям платформы: S3 удалён (1.7), язык при запуске (2.14), пауза звука/геймплея при рекламе (4.7), звук при потере фокуса (1.3), нет контекстного меню (1.6.1.8/1.6.2.7), своя прокрутка (1.10.2), `LoadingAPI.ready()` (1.19.2)
- [x] Сборка `build/yandex` + ZIP (`build/yandex.zip`, 92414 байт) — актуальны (19.08.2026)
- [x] Скриншоты 1280×720: `store/shots/yandex-*.png` (home, moves, shop, shop-skin)
- [ ] Загрузить ZIP в кабинет Яндекс Игр
- [ ] Заполнить метаданные + загрузить скриншоты 1280×720 и иконку 512×512
- [ ] Указать политику конфиденциальности
- [ ] Создать лидерборд `pirat2048_top` в кабинете
- [ ] Включить монетизацию (реклама) и подключить платёжный аккаунт
- [ ] Прогнать игру в песочнице кабинета (тест) перед отправкой
- [ ] Отправить на модерацию и дождаться одобрения

## Особенности / риски

- Игра открывается в **iframe** Яндекса: полноэкранная кнопка не работает —
  код безопасно игнорирует отказ (`requestFullscreen().catch(() => {})`).
- Прогресс сохраняется локально (localStorage) **и** в облако Яндекса
  (`pirat2048`); при конфликте объединяется (`resolveConflict` в `cloud-sync.js`).
- Для рекламы и лидерборда нужна модерация кабинета; до неё — тихий web-режим.
- `sw.js` в сборку включён (PWA), но на площадке не регистрируется.
